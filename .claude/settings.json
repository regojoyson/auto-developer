{
  "mcpServers": {
    "gitlab": {
      "command": "node",
      "args": ["mcp-servers/gitlab/index.js"],
      "env": {
        "GITLAB_BASE_URL": "${GITLAB_BASE_URL}",
        "GITLAB_TOKEN": "${GITLAB_TOKEN}",
        "GITLAB_PROJECT_ID": "${GITLAB_PROJECT_ID}"
      }
    }
  }
}
